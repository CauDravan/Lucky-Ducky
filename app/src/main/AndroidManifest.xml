<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LuckyDucky"
        tools:targetApi="31">

        <!-- Splash Activity - Launch Screen -->
        <activity
            android:name=".auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.LuckyDucky.NoActionBar"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.LuckyDucky.NoActionBar"
            android:screenOrientation="fullSensor" />

        <!-- Main Activity -->
        <activity
            android:name=".main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.LuckyDucky.NoActionBar"
            android:screenOrientation="fullSensor"
            android:launchMode="singleTop" />

        <!-- Game Activity -->
        <activity
            android:name=".game.GameActivity"
            android:exported="false"
            android:theme="@style/Theme.LuckyDucky.NoActionBar"
            android:screenOrientation="fullSensor" />

        <!-- Result Activity -->
        <activity
            android:name=".game.ResultActivity"
            android:exported="false"
            android:theme="@style/Theme.LuckyDucky.NoActionBar"
            android:screenOrientation="fullSensor" />

        <!-- Stats Activity -->
        <activity
            android:name=".game.StatsActivity"
            android:exported="false"
            android:theme="@style/Theme.LuckyDucky.NoActionBar"
            android:screenOrientation="fullSensor" />

        <!-- History Activity -->
        <activity
            android:name=".game.HistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.LuckyDucky.NoActionBar"
            android:screenOrientation="fullSensor" />

    </application>

</manifest>